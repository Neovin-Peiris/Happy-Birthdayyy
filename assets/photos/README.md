Drop your photos in this folder (e.g. `photo1.jpg`, `photo2.jpg`, ...),
then in `index.html`'s gallery section replace the matching
`<div class="photo-slot">` placeholder with:

```html
<div class="photo-slot">
  <img src="assets/photos/photo1.jpg" alt="describe the photo" />
</div>
```
